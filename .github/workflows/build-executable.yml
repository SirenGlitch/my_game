name: Build Executables

on: [push]

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Build Windows executable
      run: |
        pyinstaller --onefile --name="Game" --add-data "Assets;Assets" main.py

    - name: Upload Windows executable artifact
      uses: actions/upload-artifact@v3
      with:
        name: Windows-Build
        path: dist/Game.exe

  build-linux:
    runs-on: ubuntu-latest
  
    steps:
      - name: Log in to Docker Hub
        run: echo ${{ secrets.DOCKER_HUB_TOKEN }} | docker login -u ${{ secrets.DOCKER_HUB_USERNAME }} --password-stdin
    
      - name: Checkout repository
        uses: actions/checkout@v3
    
      - name: Build Linux executable
        run: |
          docker pull sirenglitch/my_game:beta
          docker run --rm -v $HOME/work/my_game/my_game:/app sirenglitch/my_game:beta
          
      - name: Upload Linux executable artifact
        uses: actions/upload-artifact@v3
        with:
          name: Linux-Build
          path: dist/Game